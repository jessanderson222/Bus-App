<%= form_for @route do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :bus_id %>
<%= f.collection_select :bus_id, @buses, :id, :name %>

<%= f.label :person_id %>
<%= f.collection_select :person_id, @people, :id, :name %>

<%= f.submit %>
<% end %>
